# standardize-output-path Specification

## Purpose

This change is purely a path standardization — no requirements of the `md-frontmatter-yaml` skill are modified, added, or removed. The spec `md-frontmatter-yaml` remains unchanged.

## ADDED Requirements

None. The skill's behavioral requirements are unaffected.

## MODIFIED Requirements

None. Only implementation paths and documentation are updated.

## REMOVED Requirements

None.
