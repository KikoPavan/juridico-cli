# infra/qdrant/config.yaml
# Configuração mínima do Qdrant para ambiente local (Docker Desktop)
# Montado em /qdrant/config/production.yaml dentro do container.
#
# Referência: https://qdrant.tech/documentation/guides/configuration/

service:
  host: 0.0.0.0
  http_port: 6333
  grpc_port: 6334

storage:
  storage_path: ./storage

log_level: INFO
